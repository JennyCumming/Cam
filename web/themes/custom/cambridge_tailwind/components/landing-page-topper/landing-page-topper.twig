{#
/**
 * The Top Level Topper component is at the top of the top level page. It houses the page title (h1) and an optional description.
 *
 * Title - a required string of the page title, rendered as an h1 element
 * Description - an optional string
 */
#}

<div class="py-6 bg-gray-100">
  <h1 class="text-5xl font-serif container">{{ title }}</h1>
</div>

{% if description %}
<div class="border-b-1">
  <div class="container">
    <div class="w-[600px] py-10 text-base">{{ description | raw }} </div>
  </div>
</div>
{% endif %}
