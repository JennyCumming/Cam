{#
/**
 * For a list of items with a right aligned picture
 */
#}
<a href="{{ url }}" class="block hover:bg-stone-200 px-2">
<div class="flex flex-col sm:flex-row items-start gap-4 border-b-1 py-[16px]">
  <div class="flex flex-col flex-grow gap-4">
      <p class="font-bold text-lg">{{ title }}</p>
      <p class="text-sm text-gray-600">{{ date }}</p>
  </div>
  <div class="ml-auto w-32">
    {{ image }}
  </div>
</div>
</a>
